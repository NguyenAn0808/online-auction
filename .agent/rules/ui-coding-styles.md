---
trigger: always_on
---

- Use color palelettes and variables, avoid using fixed values for colors.
- Create reusable component
- When coding layouts, ensure responsiveness on all devices, verify by browser if necessary
- follow the current UI, integrate backend for real data
